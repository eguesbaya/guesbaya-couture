{% extends 'base.html.twig' %}

{% block title %}{{ app.user.id }}- {{ app.user.firstname}} {{ app.user.lastname}}{% endblock %}

{% block body %}
<section class="container mt-5">
    <h1>{{ app.user.firstname}} {{ app.user.lastname}}</h1>


    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ user.id }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ user.email }}</td>
            </tr>
            <tr>
                <th>Roles</th>
                <td>{{ user.roles ? user.roles|json_encode : '' }}</td>
            </tr>
            <tr>
                <th>Prénom</th>
                <td>{{ user.firstname }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ user.lastname }}</td>
            </tr>
            <tr>
                <th>Adresse</th>
                <td>{{ user.address }}</td>
            </tr>
            <tr>
                <th>Vérifié?</th>
                <td><i class="bi bi-{{ user.isVerified ? 'check' : 'x' }}-square"></i></td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('user_index') }}">Retour à la liste</a>

    <a href="{{ path('user_edit', {'id': user.id}) }}">Modifier</a>

    {{ include('admin/user/_delete_form.html.twig') }}
</section>
{% endblock %}
