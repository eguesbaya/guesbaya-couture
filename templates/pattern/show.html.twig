{% extends 'base.html.twig' %}

{% block title %}Pattern{% endblock %}

{% block body %}
    <h1>Pattern</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ pattern.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ pattern.name }}</td>
            </tr>
            <tr>
                <th>Photo</th>
                <td>{{ pattern.photo }}</td>
            </tr>
            <tr>
                <th>Gender</th>
                <td>{{ pattern.gender }}</td>
            </tr>
            <tr>
                <th>FabricQuantity</th>
                <td>{{ pattern.fabricQuantity }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ pattern.Type }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('pattern_index') }}">back to list</a>

    <a href="{{ path('pattern_edit', {'id': pattern.id}) }}">edit</a>

    {{ include('pattern/_delete_form.html.twig') }}
{% endblock %}
